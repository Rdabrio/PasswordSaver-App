Rubén Dabrio

This is a simple Java project with GUI's style very basic. The idea is that you can add new Passwords with its ID (f.i. Netflix, Disney+...) and its info (the Password itself), modify existing Passwords and deleting it too. 
